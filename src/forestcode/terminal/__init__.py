"""Terminal frontend for the ForestCode interactive CLI.

Pure presentation + orchestration. Modules here may import
``forestcode.runtime`` and backend modules, but backend/runtime code must never
import ``forestcode.terminal``.
"""
